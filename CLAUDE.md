# CLAUDE.md

이 저장소에서 작업할 때 필요한 맥락. 실행 방법·검증 로그는 `README.md`에 있고, 여기에는
**왜 이걸 하는지와 넘으면 안 되는 선**만 적는다.

## 이 저장소는 무엇인가

상위 프로젝트 **"프록시 기반 악성코드 다운로드 탐지 플랫폼"**의 PoC 1 저장소다.

> 다운로드 파일을 디스크에 닿기 전 프록시가 가로채 격리 검사한 뒤, 안전한 파일만 브라우저로
> 전달하는 시스템

브라우저는 소켓에서 데이터가 도착하는 대로 디스크에 쓴다. 바이트가 브라우저에 닿는 순간 파일은
이미 존재하므로, **받고 나서 검사할 여지가 없다.** 그래서 프록시가 응답을 붙잡고 있는 것이
유일한 방법이고, 이 저장소는 그 전제가 실제로 성립하는지를 검증한다. 실패하면 상위 프로젝트의
설계 전체(5주 일정, 15개 테이블 스키마)가 무의미해지는 **게이트 항목**이다.

- 상위 설계 문서: `/Users/hangjung-macbook/Desktop/Dev/project/프록시-악성코드-탐지-플랫폼.md`
- 상위 스펙 모음: `/Users/hangjung-macbook/Desktop/Dev/project/docs/superpowers/specs/`

## 현재 상태 (2026-09-21)

| 항목 | 상태 |
|---|---|
| 포워드 프록시 + 응답 보류 + 403 교체 | ✅ curl·Chrome 실측 |
| 동시 요청 논블로킹 (`asyncio.sleep`) | ✅ |
| **HTTPS 인터셉션** | ✅ **게이트 통과** — addon 무수정, python.org 45MB, 체크섬 일치 |
| 실제 인터넷 오리진 | ✅ |
| 보류 가능 시간의 상한 | ✅ Chrome·curl 300초 초과 (Safari·Firefox·HTTPS 경로는 미측정) |
| 이벤트 대시보드 (SSE) | ✅ 검증 기준 6건 중 5건 통과 |
| **다운로드 판별 규칙** | ❌ **사용 불가 — 웹서핑 4분에 오탐 139건** |
| 스트리밍 모드 헤더 타이밍 | ❌ `stream=False`(전체 버퍼링)로만 검증 |

**PoC 1의 게이트는 닫혔다. 남은 문제는 판별 규칙이다.**

## 지금 할 일

**`docs/2026-09-21-next-detection-redesign.md`를 먼저 읽을 것.** 다운로드 판별 재설계와
`responseheaders` 전환을 **한 번에** 해야 하는 이유, 헤더를 흘려보내면 되돌릴 수 없다는 제약,
검증 기준이 거기 있다.

배경이 되는 실측 결과는 상위 저장소에 있다:

```
/Users/hangjung-macbook/Desktop/Dev/project/docs/superpowers/specs/2026-09-21-poc1-results-and-download-detection.md
/Users/hangjung-macbook/Desktop/Dev/project/docs/superpowers/specs/2026-09-21-poc-https-and-dashboard.md  (Part A·B 절차서 — 완료)
```

아래 Part A·B는 **완료됐다.** 기록으로만 남긴다.

### Part A — HTTPS 인터셉션 검증

`addons/hold_response.py`를 **한 줄도 고치지 않은 채** HTTPS에서도 `response` 훅이 불려야 정상이다.
mitmproxy가 TLS를 종료하고 평문 flow를 훅에 넘겨주기 때문이다. 코드를 고치고 싶어지면 그건 설정
문제라는 신호이니 멈추고 절차서 A.5 진단표부터 볼 것.

- 시스템 프록시를 건드리지 말고 `--proxy-server` + `--user-data-dir`로 띄운 별도 Chrome 프로필로
  먼저 검증한다 (실패해도 원복할 게 CA 하나뿐)
- Docker보다 호스트에서 직접 `mitmdump`를 띄우는 쪽이 낫다 — 컨테이너 안 `~/.mitmproxy`와 호스트
  키체인이 달라 변수가 하나 늘어난다
- CA는 **로그인 키체인이 아니라 시스템 키체인**에 등록해야 한다 (가장 흔한 실패 원인)
- 이어서 보류 상한 실측: `asyncio.sleep`을 3→10→30→60→120→300초로 올려가며 브라우저별로 언제
  끊기는지 잰다. 이 숫자가 검사 파이프라인 전체의 시간 예산이 된다

### Part B — 다운로드 이벤트 대시보드

가로챈 다운로드 정보를 실시간 화면에 띄울 수 있는지 검증한다. 절차서에 `addons/dashboard.py`
전문이 있다 (의존성은 mitmproxy 하나, SSE 서버는 표준 라이브러리 `asyncio`로 직접 구현).

- `hold_response.py`와 **동시에 쓰지 않는다** — 보류가 두 번 걸린다. `dashboard.py`가 PoC 1의
  기능을 포함한다
- 이벤트 필드는 상위 ERD의 `download_events` 컬럼명을 그대로 쓴다. 임의로 짓지 말 것 — 나중에
  서버로 옮길 때 매핑 비용이 0이 되게 하려는 의도다
- 검증 기준 중 조용한 핵심은 **"일반 웹서핑에는 행이 생기지 않는다"**. 여기가 느슨하면
  이벤트 테이블이 사실상 브라우징 이력 DB가 된다

## 넘으면 안 되는 선

상위 설계에서 확정된 제약이다. PoC라도 이 방향과 반대로 가는 코드는 쓰지 않는다.

- **로컬 에이전트는 파일 내용을 파싱하지 않는다.** PE 헤더 파싱·엔트로피·압축 해제·YARA는 전부
  검사 서버 몫이다. 에이전트는 해시 계산과 스트림 중계만 한다 — 단말의 공격 표면을 최소로 두기
  위한 결정이므로, 편의상 여기에 파싱을 넣지 않는다
- **스풀 파일은 무해해야 한다.** `UUID.tmp` 파일명, `0600` 권한·실행 비트 제거, XOR 인코딩,
  OS 인덱싱 제외. 목적은 "어떤 자동 파서(인덱서·썸네일러·백신)도 이 파일을 자기 처리 대상으로
  판단하지 않게" 만드는 것이다 (CVE-2010-2568, CVE-2017-0290 등이 근거)
- **보류는 헤더 전송 전에만 가능하다.** 응답 헤더가 브라우저로 나가고 나면 차단 응답을 만들 수
  없다. 이 타이밍을 흔드는 변경은 하지 않는다
- **검사 서버 장애 시 fail-close 고정** (2026-09-18 결정). fail-open 선택지도 정책 토글도 만들지
  않는다. 서버에 닿지 못하면 차단이다
- **식별 단위는 장비(에이전트)이며 사용자 신원은 보관하지 않는다.** MAC 주소도 저장하지 않는다
- **대시보드는 PoC 전용 구조다.** 최종은 `에이전트 → gRPC → API 서버 → SSE → 콘솔`이다. PoC
  코드를 그대로 에이전트에 남기면 로컬 에이전트가 웹 서버를 품게 되어 위 첫 번째 원칙과 충돌한다

## 비대응 범위 — 고치려 들지 말 것

검증 중 아래로 실패하면 그건 버그가 아니라 **설계상 예정된 범위 밖**이다. 고치지 말고 기록한다.

QUIC(HTTP/3) · 인증서 피닝 앱 · 시스템 프록시를 안 보는 프로그램 · Range 분할 다운로드 ·
USB/로컬 복사 · 공유 PC와 VDI(1인 1PC 전제) · 파일리스 공격과 제로데이 · 실행 시점 행위 분석(EDR 영역)

특히 피닝·HSTS로 막힌 도메인은 상위 스키마의 `bypass_domains` 초기 데이터 후보로 정리해 둔다.

## 저장소 구조

```
addons/hold_response.py   PoC 1 addon — response 훅에서 3초 보류 후 BLOCK 값에 따라 403 교체
testdata/                 테스트 파일 (Chrome이 랜덤 바이트를 자체 차단해 정상 zip 사용)
windows/                  Docker 없이 Windows 네이티브 실행용 PowerShell 스크립트
Dockerfile, docker-compose.yml   python:3.12-slim + mitmproxy 격리 실행
README.md                 실행법, curl 재현 절차, 실측 검증 로그, macOS 수동 설정 가이드
```

## 알려진 함정 (이미 한 번 밟은 것들)

- `time.sleep()`은 mitmproxy 이벤트 루프 전체를 멈춰 모든 트래픽이 직렬화된다 → `asyncio.sleep()`
- Docker 컨테이너 안에서 `localhost`는 컨테이너 자신이다 → 호스트 LAN IP 사용
- Chrome은 랜덤 바이트 파일을 자체적으로 위험 파일로 차단한다 → 정상 아카이브로 테스트
- Chrome이 일부 CDN에서 QUIC으로 빠지면 TCP 프록시를 아예 안 탄다 → `--disable-quic`으로 격리

## 작업 방식

- 커밋 메시지는 한국어, 제목 한 줄 + 본문에 "무엇을 왜"를 적는 기존 스타일을 따른다
- 검증 결과는 README에 **실측 로그 그대로** 남긴다 (수치를 요약하지 말 것 — 발표 근거가 된다)
- 검증하다 상위 설계와 어긋나는 사실을 발견하면, 이 저장소에서 고치지 말고 상위 저장소
  (`~/Desktop/Dev/project`)의 해당 스펙 문서에 반영해야 한다
